# Combined integration records

Each `atmons-<version>` compatibility tag has a matching record in this
directory. Record exact artifacts and tests actually observed; successful
builds alone are not runtime compatibility evidence.
